---
description: Specifications governing the run's stage order and shared vocabulary
globs: headroom/main.py,headroom/__main__.py,headroom/constants.py,headroom/enums.py,headroom/types.py,headroom/utils.py,headroom/output.py
alwaysApply: false
---

- [spec/architecture/overview.md](mdc:spec/architecture/overview.md) - the
  pipeline stages `main.py` runs in order, and what each one owns.
- [spec/contracts/terraform.md](mdc:spec/contracts/terraform.md) -
  `ensure_org_info_symlink` lives in `main.py` and the contract specifies it.

`constants.py`, `enums.py`, `types.py`, `utils.py`, and `output.py` hold no
behavior of their own. A value defined there is normative wherever it is
consumed, so read the contract that owns the consumer.
